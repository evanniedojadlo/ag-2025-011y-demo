kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    # You can uncomment port mappings if you want to map cluster ports to host ports
    # extraPortMappings:
    #   - containerPort: 30000
    #     hostPort: 80
    # ...
